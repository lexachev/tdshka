name: tdshka
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.9.0  # основной игровой движок
  provider: ^6.0.0  # для управления состоянием
  hive: ^2.2.0  # локальное хранилище (для сохранений)
  flutter_svg: ^2.0.0  # если нужны SVG-ассеты

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
